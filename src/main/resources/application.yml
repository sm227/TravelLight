springdoc:
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
  api-docs:
    path: /api-docs
  packages-to-scan: org.example.travellight.controller
  paths-to-match: /**

spring:
  # 기존 설정들...

# 포트원 V2 설정
portone:
  secret:
    api: ${PORTONE_API_SECRET:your_api_secret_here}
    webhook: ${PORTONE_WEBHOOK_SECRET:your_webhook_secret_here} 